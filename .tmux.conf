# Bindings
set-option -g prefix C-b
bind-key C-b send-prefix

# Vim Bindings
setw -g mode-keys vi 

# Plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'arcticicestudio/nord-tmux' # Add this line for the Nord theme

# Init TPM
run '~/.tmux/plugins/tpm/tpm'

